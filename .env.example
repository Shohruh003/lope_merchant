# Netlify dashboard → Site settings → Environment variables
# Never commit real values.

# Base URL of the LOPE backend REST API (finance endpoints live under
# /gateway/dashboard/*)
VITE_API_URL=https://api.lopestyle.uz
